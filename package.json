{
  "name": "zotero-citation",
  "version": "1.0.0",
  "description": "Typora Zotero Citation Plugin",
  "main": "main.js",
  "scripts": {
    "postinstall": "npx electron-rebuild -v 35.6.0",
    "build": "npm install && npx electron-rebuild -v 35.6.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "sql.js": "^1.13.0"
  },
  "devDependencies": {
    "electron-rebuild": "^3.2.9"
  },
  "engines": {
    "node": ">=22"
  }
}

